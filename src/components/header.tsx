import { Component } from "react";

export default class Header extends Component{
    render(){
        return (
            <header><h3 className="header-title">User's CRUD App (TypeScript ♡ ReactJS)</h3></header>
        );
    }
}